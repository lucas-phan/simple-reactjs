var React = require('react');
var PropTypes = require('prop-types');

var  = React.createClass({

    render: function() {
        return (
            <div>

            </div>
        );
    }

});

module.exports = ;
